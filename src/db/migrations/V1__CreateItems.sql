CREATE TABLE ITEMS (
                       ITEM_ID int NOT NULL PRIMARY KEY,
                       ITEM_NAME VARCHAR(50) NOT NULL,
                       TOPPING VARCHAR(30),
                       FILLING VARCHAR(30),
                       PRICE VARCHAR(30) NOT NULL
);